# Examples

- `autocatalysis_landmark.py`: end-to-end example (simulate, smooth, differentiate, detect landmark, bootstrap CI).

Run from repository root:

```bash
python examples/autocatalysis_landmark.py
```

Outputs are written to `outputs/examples/`.
